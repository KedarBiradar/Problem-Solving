#include <stdio.h>

//long long int array[10000001];

int main()
{
	long long int m,n,a,b,k,i,j,total=0;
	scanf("%lld%lld",&n,&m);

	for(i=0;i<m;i++)
	{
		scanf("%lld%lld%lld",&a,&b,&k);
		total+=(b-a+1)*k;
	}
/*
	for(i=0;i<n;i++)
	{
		total+=array[i];
	}
*/	printf("%lld\n",total/n);

	return 0;
}
